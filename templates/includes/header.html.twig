<header>
    <link rel="stylesheet" href="{{ asset('styles/header.css') }}">

    <div id="start">
        <a href="{{ path('app_homepage') }}"><img src="{{ asset('Logo-eventify.png') }}" alt="LogoEventify"></a>
        <h1>Eventify</h1>
    </div>

    <div id="end">
        {% if app.user %}
            <a href="{{ path('app_profile') }}">profil de {{ app.user.email }}</a>
            <a href="{{ path('app_logout') }}">Déconnexion</a>
        {% else %}
            <a href="{{ path('app_register') }}">Inscription</a>
            <a href="{{ path('app_login') }}">Connexion</a>
        {% endif %}
    </div>
</header>


